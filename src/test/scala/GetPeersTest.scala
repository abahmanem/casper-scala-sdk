import org.scalatest.funsuite.AnyFunSuite

/*
import com.casper.sdk._
import com.casper.sdk.core._
import com.casper.sdk.rpc.result._
import com.casper.sdk.util.implicits.idInstance

class GetPeersTest extends AnyFunSuite {

  val transport = new HttpTransport("http://65.21.202.120:7777/rpc",new JsonConverterByClass,100000,
    100000)
  val cspr = new SDK(transport)

  test("peers not empty") {
    val peers : GetPeersResult = cspr.get_info_get_peers(Seq("f"))
    assert (peers.result  != Option.empty)
    assert (peers.error  == Option.empty)
  }
}*/
