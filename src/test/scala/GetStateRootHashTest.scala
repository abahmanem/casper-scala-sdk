import org.scalatest.funsuite.AnyFunSuite



import com.casper.sdk.util.implicits.idInstance

class GetStateRootHashTest extends AnyFunSuite {
/*
  val transport = new HttpTransport("http://65.21.202.120:7777/rpc",new JsonConverterByClass,1000,1000)
  val cspr = new SDK(transport)

  test("state root hash not empty") {
    val stateRootHash : GetStateRootHashResult = cspr.get_chain_get_state_root_hash(Seq("f"))
    assert (stateRootHash.result.get.state_root_hash  != "")
    assert (stateRootHash.error  == Option.empty)
  }*/
}
